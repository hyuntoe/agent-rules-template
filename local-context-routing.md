# 로컬 컨텍스트 라우팅 규칙

이 문서는 대용량 입력을 클라우드 에이전트에 통째로 보내기 전에 로컬 모델로 관련 부분만 고르는 규칙을 정의합니다. 이 저장소를 다른 프로젝트에 병합할 때, 해당 프로젝트의 장비와 모델 상태에 맞게 모델 이름만 조정합니다.

## 목적

- 긴 로그, 긴 단일 파일, 긴 문서에서 필요한 라인만 먼저 찾습니다.
- 클라우드 LLM에 보내는 토큰과 외부 전송량을 줄입니다.
- 원문 의미를 요약으로 잃지 않도록, 요약문이 아니라 원문 라인 범위를 넘깁니다.

## 기본 원칙

- 로컬 라우터는 판단 보조 도구입니다. 최종 구현 판단, 보안 판단, 아키텍처 판단을 대체하지 않습니다.
- 라우터 출력은 원문 라인 후보로 취급합니다. 중요한 작업에서는 후보 주변 문맥을 사람이든 상위 에이전트든 다시 확인합니다.
- 프로젝트 전체 설계, 여러 파일 간 계약, 보안 감사처럼 넓은 맥락이 필요한 작업에는 라우터를 기본 경로로 쓰지 않습니다.
- 짧은 파일, 이미 충분히 작은 로그, 명확한 에러 한 줄에는 라우터를 쓰지 않습니다.

## 라우터 사용 경계값

아래 기준은 기본 운영값입니다. 다른 프로젝트에 이 템플릿을 병합하면서 기준을
바꿀 수 있지만, 바꾼 경우에는 프로젝트의 `README.md`나 `docs/`에 변경한 줄 수,
토큰 수, 모델 이름과 근거를 명시합니다. 근거 없이 "프로젝트 상황에 맞게 판단"이라고만
남기지 않습니다.

- 500줄 미만이고 에러 위치나 검색 키워드가 명확하면 직접 읽습니다.
- 500줄 이상 2000줄 미만이고 찾을 심볼, 에러명, 설정 키가 명확하면 `rg` 또는 직접 검색을 먼저 씁니다.
- 2000줄 이상이거나 원인 후보가 불명확하면 로컬 라우터를 먼저 고려합니다.
- 32768 tokens 안에 들어갈 가능성이 크면 `small-router`를 우선 씁니다.
- 32768 tokens를 넘을 가능성이 있거나 64K 이상 컨텍스트가 필요한 흐름이면 `long-router`를 씁니다.
- 여러 파일의 설계 관계를 판단해야 하면 라우터보다 graphify, 직접 읽기, 테스트, 리뷰를 우선합니다.

## 권장 사용처

- 수천 줄 이상의 빌드 로그에서 실제 실패 원인을 찾을 때.
- 긴 테스트 출력에서 첫 실패, 관련 스택, 반복 실패 패턴을 찾을 때.
- 긴 단일 소스 파일에서 특정 함수, 예외, 설정, 호출부 후보를 찾을 때.
- 긴 규칙 문서 묶음에서 현재 작업에 직접 필요한 조항만 먼저 찾을 때.

## 비권장 사용처

- 작은 입력을 다룰 때.
- 라우터가 고른 라인만 보고 바로 코드를 고치는 경우.
- 민감 정보가 포함된 입력을 로컬 보관 정책 없이 재가공하는 경우.
- 여러 프로젝트 문서를 한꺼번에 병합하는 정책 판단.

## 모델 프로파일

기본값은 Ollama를 기준으로 둡니다. WSL CLI, Codex Desktop, agy IDE, Claude Desktop처럼 실행 진입점이 여러 개여도 로컬 라우터 API를 한 곳으로 맞추기 쉽기 때문입니다.

### long-router

- 모델: `gemma4:e2b-it-q4_K_M`.
- 런타임: Ollama.
- 확인된 컨텍스트: 131072 tokens.
- 용도: 64K 이상 컨텍스트가 필요한 hermes, 긴 로그, 긴 문서 라우팅.
- 운영: 필요할 때만 로드하고, 작업 뒤에는 모델을 내려 VRAM을 비웁니다.

```powershell
ollama run gemma4:e2b-it-q4_K_M --keepalive 60s
ollama stop gemma4:e2b-it-q4_K_M
ollama ps
```

### small-router

- 모델: `qwen2.5-coder:3b`.
- 런타임: Ollama.
- 확인된 컨텍스트: 32768 tokens.
- 용도: 짧은 로그, 중간 크기 소스 파일, 빠른 반복 라우팅.
- 운영: 32K 안에 들어가는 입력에는 이 모델을 우선 고려합니다.

```powershell
ollama run qwen2.5-coder:3b --keepalive 60s
ollama stop qwen2.5-coder:3b
ollama ps
```

## Windows와 WSL 운영

- Ollama는 Windows에서 실행하고, WSL CLI들은 Windows Ollama API를 호출하는 구성을 기본으로 둡니다.
- WSL에서 `localhost:11434`가 닿지 않으면 Windows host IP를 찾아 `OLLAMA_HOST` 또는 도구별 URL 설정에 반영합니다.
- VRAM 관리는 Ollama 서버 종료보다 모델 unload로 처리합니다.
- LM Studio는 GUI 실험과 백업 런타임으로 둘 수 있지만, 자동화 기본 경로는 Ollama로 둡니다.

## Ollama 장애와 지연 대응

LCR은 쿼터 절약 장치이지 작업을 막는 필수 관문이 아닙니다. Ollama나 모델이
준비되지 않았으면 작게 폴백하고, 어떤 폴백을 했는지 보고합니다.

1. `ollama list`로 필요한 모델이 있는지 먼저 확인합니다.
2. 모델이 없으면 사용자 승인 없이 대형 모델을 다운로드하지 않습니다.
3. `long-router`가 없고 `small-router`가 있으면 32768 tokens 이하 입력만 `small-router`로 처리합니다.
4. Ollama 응답이 30초 이상 없으면 한 번만 재시도합니다.
5. 재시도도 느리면 `rg`, `git`, graphify, 직접 원문 범위 읽기로 폴백합니다.
6. 라우터 없이 클라우드 LLM에 보내야 하면 입력을 수동으로 줄이고, LCR을 쓰지 못한 이유를 함께 보고합니다.
7. WSL에서 Windows Ollama API에 연결되지 않으면 `localhost:11434`, Windows host IP, `OLLAMA_HOST`를 순서대로 확인합니다.
8. 다운로드, 설치, 장시간 pull, 서버 설정 변경은 사용자 승인 후 진행합니다.

## 에이전트 작업 흐름

1. 입력이 큰지 먼저 판단합니다.
2. 큰 입력이면 `small-router` 또는 `long-router` 중 하나를 고릅니다.
3. 라우터로 관련 라인 후보를 찾습니다.
4. 후보 라인 주변 원문을 다시 읽습니다.
5. 필요한 원문 조각만 클라우드 에이전트 프롬프트나 작업 문서에 포함합니다.
6. 작업 완료 후 `ollama stop <model>`로 VRAM을 비웁니다.

## 템플릿 병합 시 조정 항목

다른 프로젝트에 이 규칙을 병합할 때는 아래 항목만 프로젝트 상황에 맞게 바꿉니다.

- 실제 설치된 Ollama 모델 이름.
- WSL에서 Windows Ollama API에 접근하는 주소.
- hermes, codex, claude, agy 중 어떤 CLI가 주 진입점인지.
- 민감 로그와 고객 데이터에 대한 로컬 보관 정책.
- 프로젝트별 대용량 로그 위치와 테스트 출력 위치.
