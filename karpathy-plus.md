# karpathy-plus — 공통 개발 규칙

Andrej Karpathy가 공개한 개발 원칙(그의 `CLAUDE.md`)을 기반으로, 본 프로젝트에
맞게 변형한 공통 개발 규칙. **모든 에이전트(Claude·codex·agy)가 따른다.**
이 파일이 개발 규칙의 단일 진실 소스(SSOT)이며, 각 진입 파일(`CLAUDE.md`·
`AGENTS.md`)은 이 문서를 가리키기만 한다.

흔한 LLM 코딩 실수를 줄이기 위한 행동 지침입니다. 프로젝트별 지침과 함께 사용합니다.

**Tradeoff:** 이 지침은 속도보다 신중함을 우선합니다. 아주 단순한 작업에서는 상황에 맞게 판단합니다.

### 1. 코딩 전에 먼저 생각합니다.
**추정하지 말고, 혼란을 숨기지 말고, 트레이드오프를 드러냅니다.**

구현 전에 다음을 지킵니다.
- 가정을 명시합니다. 확실하지 않으면 질문합니다.
- 여러 해석이 가능하면 조용히 하나를 고르지 말고 선택지를 제시합니다.
- 더 단순한 접근이 있으면 말합니다. 필요하면 합리적으로 반론합니다.
- 불명확한 점이 있으면 멈추고, 무엇이 불분명한지 설명한 뒤 질문합니다.

### 2. 단순성을 우선합니다.
**문제를 해결하는 최소한의 코드만 작성합니다. 추측성 기능은 넣지 않습니다.**

- 요청받지 않은 기능은 추가하지 않습니다.
- 단일 용도 코드에 추상화를 만들지 않습니다.
- 요청받지 않은 유연성이나 설정 가능성을 넣지 않습니다.
- 불가능한 상황을 위한 과도한 예외 처리를 넣지 않습니다.
- 200줄이 50줄로 줄어들 수 있다면 단순화합니다.

스스로 묻습니다. "이걸 시니어 엔지니어가 과하다고 볼까?" 그렇다면 단순화합니다.

### 3. 외과적으로 수정합니다.
**필요한 부분만 건드리고, 자신이 만든 부산물만 정리합니다.**

기존 코드를 수정할 때:
- 인접한 코드, 주석, 포맷을 멋대로 개선하지 않습니다.
- 고장나지 않은 것을 리팩터링하지 않습니다.
- 내 취향과 달라도 기존 스타일을 맞춥니다.
- 관련 없는 죽은 코드를 발견하면 언급만 하고 삭제하지 않습니다.

내 변경으로 생긴 고아 항목이 있으면:
- 내 변경 때문에 안 쓰게 된 import, 변수, 함수는 제거합니다.
- 기존에 있던 죽은 코드는 요청받지 않으면 제거하지 않습니다.

기준은 이것입니다. 모든 변경 줄이 사용자의 요청과 직접 연결되어야 합니다.

### 4. 목표 중심으로 실행합니다.
**성공 기준을 정의하고, 검증될 때까지 반복합니다.**

작업을 검증 가능한 목표로 바꿉니다.
- "검증 추가" → "잘못된 입력 테스트를 먼저 쓰고 통과시킨다."
- "버그 수정" → "재현 테스트를 먼저 쓰고 통과시킨다."
- "X 리팩터링" → "전후 테스트가 모두 통과함을 확인한다."

다단계 작업에서는 짧은 계획을 적습니다.

```text
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```

강한 성공 기준은 스스로 끝까지 진행할 수 있게 하고, "되게 만들기" 같은 약한 기준은 추가 설명을 계속 필요로 하게 만듭니다.

### 5. 한국어 문장 끝에 콜론을 쓰지 않습니다.
**한국어 문장은 콜론이 아니라 마침표, 물음표, 느낌표로 끝냅니다.**

사용자가 한국어로 쓰면 출력도 한국어입니다.
- 다음 줄에 목록이나 예시가 와도 문장 끝을 `:`로 끝내지 않습니다.
- 영어 문서 영향으로 한국어에 콜론 습관이 새는 경우를 잡아냅니다.
- 기준은 이것입니다. 한국어 문장 종결은 `.`, `?`, `!` 중 하나여야 하고 `:`가 아니어야 합니다.
- 코드, 키-값, 라벨 안의 콜론은 허용합니다. 문장 끝 콜론만 금지합니다.

### 6. 새 소스 파일 첫 줄에는 한국어 역할 주석을 둡니다.
**새 소스 파일 첫 줄에는 파일 역할을 설명하는 한 줄 한국어 주석을 둡니다.**

새 파일을 만들 때:
- TypeScript/JavaScript: `// 사용자 인증 상태를 관리하는 Context Provider`
- Python: `# KIS API 호출을 비동기로 래핑하는 클라이언트`
- SQL: `-- 일별 집계 결과를 저장하는 머티리얼라이즈드 뷰`
- `'use client'`, `'use server'`, shebang 같은 필수 지시문 바로 아래에 둡니다.
- `*.config.ts`, `package.json` 같은 설정 파일은 제외합니다.

### 7. 단일 플랜 파일을 만듭니다.
**비단순 작업 전에는 단일 플랜 파일 하나를 만들고, 그 전에는 코딩을 시작하지 않습니다.**

이 파일에는 다음을 함께 둡니다.
- **Plan** — 무엇을 왜 만드는지.
- **Checklist** — 구체 작업 체크박스.
- **Context Notes** — 작업 중 내린 결정과 이유.

`checklist.md`, `context-notes.md`처럼 이를 분리한 별도 파일은 만들지 않습니다. 체크리스트와 컨텍스트 노트는 반드시 같은 단일 플랜 파일 안에서 함께 관리합니다.

사용자가 거친 계획만 주고 바로 코딩을 시작하라고 하면, 먼저 이 통합 플랜 파일을 만들지 물어봐야 합니다. 다음 세션도 이 파일 하나만 보고 이어서 작업할 수 있어야 합니다.

플랜 파일의 모든 체크박스가 완료되면 `docs/history/project/YYYY-MM-DD_주제.md` 형식으로 이동합니다. 루트에 남겨두지 않습니다. (docs 관리 상세는 `workflow-rules.md`.)

### 8. 완료 전에 테스트를 실행합니다.
**코드를 건드렸다면 끝났다고 말하기 전에 테스트를 돌립니다.**

- `npm test`, `pytest`, `cargo test` 같은 프로젝트 테스트 명령을 실행합니다.
- 테스트가 통과하면 결과를 보고하고, 실패하면 고치고 다시 돌립니다.
- 테스트 환경이 없으면 최소한 빌드나 컴파일을 확인합니다.
- 사용자가 완료 신호를 보내기 전에 선제적으로 검증합니다.

### 9. 의미 단위로 커밋합니다.
**하나의 논리적 변경이 끝나면 커밋합니다. 사용자가 말할 때까지 기다리지 않습니다.**

- 기준은 이것입니다. "이 커밋을 한 문장으로 설명할 수 있는가?" 가능하면 커밋하고, 아니면 더 쪼갭니다.
- 좋은 예: `auth 미들웨어 추가`.
- 나쁜 예: `auth 추가하고 UI도 고치고 버그도 수정`.
- 서로 무관한 변경을 한데 쌓아 롤백 불가능하게 만들지 않습니다.
- 커밋 자체를 위해 커밋하지 말고, 의미 있는 단위로 커밋합니다.

### 10. 에러를 읽고, 추측하지 않습니다.
**실제 에러와 로그를 읽고, 기억에 의존한 패턴 매칭으로 고치지 않습니다.**

실패했을 때:
- 전체 에러 메시지와 스택 트레이스를 읽습니다.
- 예상한 로그가 아니라 실제 로그 출력을 봅니다.
- 원인을 확인하기 전에 흔한 해결책을 먼저 적용하지 않습니다.
- 불명확하면 print나 log를 추가해 상태를 확인한 뒤 수정합니다.
