# 공통 스킬 사용 규칙

이 문서는 이 템플릿이 제공하는 공통 스킬을 언제 사용할지 정의합니다. 실제 스킬 원본은 `skills/` 디렉토리에 두고, Codex CLI, Claude Code CLI, agy CLI의 스킬 디렉토리에 복제해 사용합니다.

## 기본 원칙

- 스킬은 세션 시작마다 항상 읽히는 규칙을 줄이기 위한 온디맨드 절차입니다.
- 진입 파일(`AGENTS.md`, `CLAUDE.md`)에는 스킬 본문을 복붙하지 않고, 이 문서와 `skills/` 원본을 가리킵니다.
- 큰 입력, 구조 탐색, 클라우드 LLM 호출 전 점검처럼 반복되는 절차는 스킬로 처리합니다.
- 모든 판단을 스킬에 숨기지 않습니다. 보안, 데이터 손실, 아키텍처 판단은 원문 확인과 검증을 거칩니다.

## 제공 스킬

### `context-budget-gate`

클라우드 LLM 호출 전에 컨텍스트 예산을 점검합니다. 다음 상황에서 사용합니다.

- Claude, Codex, agy 같은 클라우드 LLM에 넓은 작업을 맡기기 전.
- 긴 로그, 긴 파일, 아키텍처 질문, 멀티파일 변경을 다루기 전.
- 모델, effort, MCP, plugin 변경이 캐시를 깨는지 고민해야 할 때.
- Claude subagent를 부르기 전에 로컬 처리로 충분한지 판단할 때.

### `local-context-router`

Ollama 기반 Local Context Router입니다. 다음 상황에서 사용합니다.

- 2000줄 이상 로그나 파일을 다룰 때.
- 32768 tokens를 넘을 가능성이 있는 입력을 클라우드 LLM에 보내기 전.
- hermes처럼 64K 이상 컨텍스트가 필요한 흐름을 준비할 때.
- 실패 원인이 불명확한 긴 테스트 출력이나 빌드 로그를 줄일 때.

### `graphify-code-map`

graphify로 코드베이스 구조를 먼저 좁힙니다. 다음 상황에서 사용합니다.

- 아키텍처, 모듈 관계, 호출 경로, 핵심 추상화를 묻는 질문.
- "어디를 고쳐야 하나?"처럼 후보 파일을 먼저 좁혀야 하는 작업.
- 낯선 저장소에서 클라우드 LLM이 전체 구조를 재발견하지 않게 해야 할 때.

## 설치 위치

- Codex CLI: `~/.codex/skills/`.
- Claude Code CLI: `~/.claude/skills/`.
- agy CLI: `~/.agents/skills/`.

스킬 원본은 이 저장소의 `skills/` 아래를 기준으로 관리합니다. 스킬 내용을 고치면 세 설치 위치에 다시 복제합니다.

## 운영 순서

클라우드 LLM을 호출하기 전에 기본적으로 아래 순서를 따릅니다.

1. `rg`, `git`, 테스트 도구로 직접 좁힙니다.
2. 코드 구조 문제면 `graphify-code-map`을 사용합니다.
3. 긴 입력이면 `local-context-router`를 사용합니다.
4. 클라우드 호출 전 `context-budget-gate`로 남은 컨텍스트와 캐시 위험을 점검합니다.
5. 클라우드 LLM에는 후보 파일, 원문 라인 범위, 명확한 목표와 성공 기준만 넘깁니다.

## 프로젝트별 조정

프로젝트가 기준을 바꾸려면 `README.md`나 `docs/`에 변경한 스킬 이름, 모델 이름, 줄 수, 토큰 수, 사용 근거를 남깁니다. 근거 없이 "프로젝트 상황에 맞게 사용"이라고만 남기지 않습니다.
