# 규칙 문서 미커밋 작업 완결

## Plan

현재 작업 트리의 의미 있는 문서 변경을 보존하고, 우발적인 CRLF 줄바꿈 변경은
제거한 뒤 하나의 검증된 커밋으로 `main`과 원격 저장소에 반영한다.

## Checklist

- [x] 의미 변경과 줄바꿈 전용 변경을 분리한다.
- [x] 줄바꿈 전용 변경을 기존 LF로 정규화한다.
- [x] README, ignore 규칙과 개인 프로젝트 병합 백로그를 검증한다.
- [x] 완료 기록으로 이동할 수 있는 상태로 정리한다.

## Context Notes

- 작업 브랜치는 `chore/finalize-rule-docs`이며 문서 변경과 검증은 완료되었다.
- 원래 작업 트리에는 추적 파일 21개의 변경과 새 백로그 파일 하나가 있었다.
- `git diff --ignore-cr-at-eol` 기준 실제 의미 변경은 `.gitignore`와 `README.md`뿐이었다.
- 나머지 추적 파일 19개는 내용이 동일하고 LF가 CRLF로 바뀐 상태이므로 커밋 잡음을 막기 위해 LF로 되돌린다.
- `docs/backlog/next.md`는 개인 프로젝트 10개에 공통 규칙을 추가 병합하는 새 백로그이다.
- 검증 결과 실제 커밋 대상은 `.gitignore`, `README.md`, 새 백로그와 이 완료 기록뿐이다.
