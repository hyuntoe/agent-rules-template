# 룰 적용 경계 예시 추가 플랜

## Plan

이 작업의 목표는 기존 규칙 문서의 원칙을 바꾸지 않으면서, 해석 차이가 생기기 쉬운 경계에 짧은 예시를 추가하는 것입니다.

이번 작업에서는 다음 세 가지를 보강합니다.
- 비단순 작업으로 판단해 플랜 파일이 필요한 경우 예시.
- `docs/backlog` → `docs/plans` → `docs/history` 이동 예시.
- 단일 에이전트 작업과 멀티에이전트 작업의 경계 예시.

예시는 새 규칙을 만들기보다 기존 규칙 바로 아래에 짧게 붙여, 템플릿의 단순성을 유지합니다.

## Checklist

- [x] `workflow-rules.md`에 docs 흐름 예시를 추가합니다.
- [x] `multi-agents-orchestration.md`에 단일 에이전트와 멀티에이전트 경계 예시를 추가합니다.
- [x] `karpathy-plus.md`에 비단순 작업과 플랜 파일 기준 예시를 추가합니다.
- [x] `README.md`는 수정하지 않기로 결정합니다.
- [x] 변경 내용을 검토합니다.
- [x] 검증 명령을 실행합니다.
- [x] 커밋합니다.

## Context Notes

- 예시는 과하지 않게 각 문서에 1개 섹션씩만 추가합니다.
- README에는 규칙 본문을 다시 복제하지 않고, 필요한 경우 위치만 안내합니다.
- 실제로는 README 연결 문구 없이도 문서 책임 경계가 충분히 명확해 보여 수정하지 않기로 했습니다.
- 문서 전용 변경이므로 검증은 `git diff --check`와 최종 내용 검토로 대신합니다.
- 검증 명령으로 `git diff --check`를 실행했고 통과했습니다.
