# docs 디렉토리 사용 워크플로우를 정의합니다.

# Docs Workflow

이 문서는 `docs/backlog`, `docs/plans`, `docs/history`를 어떻게 구분해 사용할지에 대한 프로젝트 기준 문서입니다.

## 기본 원칙

- 작업 상태에 따라 문서를 한 군데에만 둡니다.
- 같은 작업의 활성 기록과 완료 기록을 여러 디렉토리에 중복 보관하지 않습니다.
- 비단순 작업은 시작 전에 작업별 플랜 파일 하나를 만들고, 완료되면 이력 디렉토리로 이동합니다.
- 여러 활성 작업이 있으면 플랜 파일도 여러 개 둘 수 있지만, 같은 브랜치나
  worktree에서 `active` 상태로 쓰는 작업은 하나만 둡니다.

## 단순 작업과 비단순 작업 판정

아래 기준은 기본 운영값입니다. 다른 프로젝트에 이 템플릿을 병합할 때 기준을
바꿀 수 있지만, 바꾼 경우에는 프로젝트의 `README.md`나 `docs/`에 변경한 숫자와
근거를 명시합니다. 근거 없이 "프로젝트 상황에 맞게 판단"이라고만 남기지 않습니다.

다음 조건 중 2개 이상이 참이면 비단순 작업으로 보고 `docs/plans/`에 작업별
플랜 파일을 만듭니다.

- 수정 파일이 2개 이상입니다.
- 공통 규칙, 워크플로우, 에이전트 행동을 바꿉니다.
- 다른 프로젝트에 병합해 사용할 템플릿 의미가 바뀝니다.
- 검증 명령이나 검토 단계가 2개 이상 필요합니다.
- 다음 세션이 결정 이유를 알아야 안전하게 이어갈 수 있습니다.
- 실패 시 되돌릴 영향 범위가 넓거나 여러 사용자에게 전파됩니다.

다음 조건이 모두 참이면 단순 작업으로 보고 플랜 없이 처리할 수 있습니다.

- 수정 파일이 1개 이하입니다.
- 새 규칙이나 정책을 만들지 않습니다.
- 다른 프로젝트에 전파될 의미 변화가 없습니다.
- 검증이 diff 확인이나 단일 명령으로 끝납니다.
- 실패해도 롤백이 쉽고 영향 범위가 작습니다.

## 디렉토리별 역할

### `docs/backlog/`

- 아직 시작하지 않은 작업 아이디어와 후속 과제를 둡니다.
- 가까운 작업은 `docs/backlog/next.md`에 둡니다.
- 장기 과제나 구조 개편 아이디어는 `docs/backlog/later.md`에 둡니다.
- 개별 대화 기록이나 검토 메모가 필요하면 `docs/backlog/YYYY-MM-DD_주제.md` 형식으로 둡니다.

### `docs/plans/`

- 지금 진행 중인 활성 작업의 플랜 파일을 둡니다.
- 플랜 파일은 작업당 하나만 만듭니다.
- 플랜 파일 안에는 `Plan`, `Checklist`, `Context Notes`를 함께 둡니다.
- `checklist.md`, `context-notes.md`처럼 분리 파일을 추가로 만들지 않습니다.
- **Gemini/Antigravity 아티팩트 싱크**: 에이전트 내부 아티팩트인 `implementation_plan.md` 및 `task.md` 사본은 이곳에 동일하게 작성하여 동기화합니다.

### `docs/history/`

- 완료된 작업 기록만 둡니다.
- 프로젝트 작업 플랜은 `docs/history/project/YYYY-MM-DD_주제.md` 형식으로 이동합니다.
- **Gemini/Antigravity 아티팩트 싱크**: 에이전트 완료 후 생성되는 `walkthrough.md` 사본은 `docs/history/project/` 하위로 이동하여 영구 보존합니다.
- 세션 로그, 위키 온톨로지 등 다른 역사성 문서는 기존 하위 디렉토리 체계를 따릅니다.

## 작업 흐름

1. 아직 시작하지 않은 아이디어는 `docs/backlog/`에 둡니다.
2. 실제로 착수하는 순간 `docs/plans/`에 해당 작업의 플랜 파일 하나를 만듭니다.
3. 작업 중에는 같은 플랜 파일 안의 체크리스트와 컨텍스트 노트를 계속 갱신합니다.
4. 체크박스가 모두 완료되면 플랜 파일을 `docs/history/project/`로 이동합니다.
5. 완료된 작업은 `docs/plans/`나 루트에 남겨두지 않습니다.

## 여러 활성 플랜

`docs/plans/`에는 서로 다른 활성 작업의 플랜을 여러 개 둘 수 있습니다. 이는
작업이 장기간 중단되거나 여러 프로젝트성 과제가 병행될 때 현재 상태와 다음
행동을 보존하는 데 유리합니다.

복수 활성 플랜의 장점은 다음과 같습니다.

- 한 작업이 승인·자료·리밋을 기다리는 동안 다른 작업을 진행할 수 있습니다.
- 각 작업의 결정과 남은 일을 분리해 장기 중단 후에도 복원하기 쉽습니다.
- 서로 다른 브랜치·worktree에서 독립 작업을 병렬 진행할 수 있습니다.

복수 활성 플랜의 위험은 다음과 같습니다.

- 한 작업 트리에서 여러 플랜을 번갈아 수정하면 커밋에 서로 다른 작업이 섞일 수 있습니다.
- 현재 세션이 어느 플랜을 수행하는지 혼동하면 잘못된 브랜치나 오래된 상태를 수정할 수 있습니다.
- 공통 파일을 수정하는 작업끼리는 머지 충돌과 중복 구현이 생길 수 있습니다.
- 오래 멈춘 플랜이 `docs/plans/`에 남아 실제 활성 작업과 구분되지 않을 수 있습니다.

따라서 다음 조건을 지킵니다.

- **작업당 플랜 하나, 플랜당 브랜치 하나**를 기본으로 합니다. 병렬 쓰기가
  필요하면 브랜치마다 별도 worktree를 사용합니다.
- **같은 브랜치·worktree에서 `active` 상태인 플랜은 하나만 둡니다.**
  `waiting`·`paused` 플랜은 함께 둘 수 있지만, 다시 시작할 때 현재 active
  플랜을 먼저 체크포인트하고 상태를 바꿉니다.
- 플랜의 Context Notes에 작업 브랜치 또는 worktree 경로, 현재 상태
  (`active`, `waiting`, `paused`)를 기록합니다.
- 다른 활성 플랜으로 전환하기 전에 현재 변경을 검증하고 의미 단위로 커밋하거나,
  커밋할 수 없는 이유와 미커밋 파일을 플랜에 기록합니다.
- 공통 파일을 수정하는 활성 작업이 겹치면 병렬 진행하지 않고 순서를 정합니다.
- 더 이상 진행하지 않는 플랜은 backlog로 되돌리거나, 완료·취소 근거를 남기고
  history로 이동합니다.

## 예시

### 예시 1. backlog에서 plans로 옮기는 시점

- `docs/backlog/next.md`에 "배포 스크립트 롤백 절차 정리"가 적혀 있는 상태는 아직 착수 전입니다.
- 실제로 파일을 읽고, 적용 범위를 정하고, 작업 체크리스트를 쓰기 시작하면 그 시점부터는 활성 작업이므로 `docs/plans/YYYY-MM-DD_주제.md`로 분리합니다.
- 같은 내용을 backlog와 plans에 동시에 두지 않습니다.

### 예시 2. plans에서 history로 옮기는 시점

- `docs/plans/2026-06-05_auth-guard.md`에서 체크박스를 모두 완료하고, 관련 테스트나 검증 명령까지 끝났으면 활성 작업이 아닙니다.
- 이때 파일을 `docs/history/project/2026-06-05_auth-guard.md`로 이동합니다.
- 구현은 끝났지만 검증이 남았으면 아직 history로 옮기지 않습니다.

## 브랜치 방침

- **기본 브랜치는 `main`.** `master`는 쓰지 않습니다(차별적 함의가 있는 용어를 피하는 업계 표준).
- **`main`은 보호 브랜치** — 직접 커밋하지 않고 안정 상태만 둡니다. 특히 headless 워커는 `main`을 직접 건드리지 않습니다.
- **작업(플랜) 단위로 feature 브랜치**를 만듭니다(플랜 파일과 1:1, 예 `plan/<주제>`). 작업이 끝나면 PM이 검토 후 `main`에 머지합니다.
- **worktree 격리**는 멀티에이전트가 병렬로 쓰기를 할 때 도입합니다(워커마다 별도 worktree). 단독·순차 작업 단계에서는 보류합니다.
- 커밋은 의미 단위로 합니다(`karpathy-plus.md` 9번).

## 에이전트용 강제 규칙

- `AGENTS.md`, `CLAUDE.md`를 읽는 에이전트는 작업 전 이 문서를 먼저 확인합니다.
- `docs` 관련 문서를 새로 만들거나 이동할 때는 이 문서의 역할 구분을 우선 적용합니다.
- 다른 문서와 이 문서가 충돌하면, `docs` 워크플로우에 대해서는 이 문서를 기준으로 해석합니다.
