# docs 디렉토리 사용 워크플로우를 정의합니다.

# Docs Workflow

이 문서는 `docs/backlog`, `docs/plans`, `docs/history`를 어떻게 구분해 사용할지에 대한 프로젝트 기준 문서입니다.

## 기본 원칙

- 작업 상태에 따라 문서를 한 군데에만 둡니다.
- 같은 작업의 활성 기록과 완료 기록을 여러 디렉토리에 중복 보관하지 않습니다.
- 비단순 작업은 시작 전에 플랜 파일 하나를 만들고, 완료되면 이력 디렉토리로 이동합니다.

## 디렉토리별 역할

### `docs/backlog/`

- 아직 시작하지 않은 작업 아이디어와 후속 과제를 둡니다.
- 가까운 작업은 `docs/backlog/next.md`에 둡니다.
- 장기 과제나 구조 개편 아이디어는 `docs/backlog/later.md`에 둡니다.
- 개별 대화 기록이나 검토 메모가 필요하면 `docs/backlog/YYYY-MM-DD_주제.md` 형식으로 둡니다.
- backlog 항목이 착수 단계로 넘어가면 해당 항목을 backlog 파일에서 삭제합니다.

### `docs/plans/`

- 지금 진행 중인 활성 작업의 플랜 파일을 둡니다.
- 플랜 파일은 작업당 하나만 만듭니다. 동시에 여러 작업이 진행되면 작업 수만큼 파일이 있을 수 있지만 최소로 유지합니다.
- 파일명은 `주제.md` 형식으로 합니다. 날짜는 `docs/history/`로 이동할 때 붙입니다.
- 플랜 파일 안에는 `Plan`, `Checklist`, `Context Notes`를 함께 둡니다.
- `checklist.md`, `context-notes.md`처럼 분리 파일을 추가로 만들지 않습니다.

### `docs/history/`

- 완료된 작업 기록만 둡니다.
- 프로젝트 작업 플랜은 `docs/history/project/YYYY-MM-DD_주제.md` 형식으로 이동합니다.
- 세션 로그, 위키 온톨로지 등 다른 역사성 문서는 기존 하위 디렉토리 체계를 따릅니다.

## 작업 흐름

1. 아직 시작하지 않은 아이디어는 `docs/backlog/`에 둡니다.
2. 실제로 착수하는 순간 `docs/plans/주제.md`로 플랜 파일을 만들고, backlog에서 해당 항목을 삭제합니다.
3. 작업 중에는 같은 플랜 파일 안의 체크리스트와 컨텍스트 노트를 계속 갱신합니다.
4. 체크박스가 모두 완료되면 검증(테스트 또는 빌드)을 실행합니다.
5. 검증 통과 후 커밋합니다. 이후 사용자에게 푸시 여부를 묻고 승인받으면 푸시합니다.
6. 플랜 파일을 `docs/history/project/YYYY-MM-DD_주제.md`로 이름을 바꾸어 이동합니다.
7. 완료된 작업은 `docs/plans/`나 루트에 남겨두지 않습니다.

## 에이전트용 강제 규칙

- `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`를 읽는 에이전트는 작업 전 이 문서를 먼저 확인합니다.
- `docs` 관련 문서를 새로 만들거나 이동할 때는 이 문서의 역할 구분을 우선 적용합니다.
- 다른 문서와 이 문서가 충돌하면, `docs` 워크플로우에 대해서는 이 문서를 기준으로 해석합니다.
