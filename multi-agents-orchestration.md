# 멀티에이전트 협업 워크플로

이 문서는 사용자가 진입한 인터랙티브 에이전트를 PM/오케스트레이터로 두고,
필요할 때 다른 에이전트를 워커로 호출하는 협업 규칙을 정의한다. `docs/`
디렉토리 관리 규칙은 이 문서가 아니라 `workflow-rules.md`를 따른다(역할이
다름).

> 상태: **검증 단계** (2026-06-04). 1-hop·2-hop headless 호출은 검증됨. 실전
> 다단계 파이프라인은 미검증이므로 점진적으로 확장하며, 글로벌(~/.claude)
> 승격은 보류한다.

---

## 1. 적용 범위 — 단순 작업은 단일 에이전트

**기본값은 단일 에이전트다.** 멀티에이전트는 비용·복잡도·핸드오프 손실이
크므로 복잡한 작업에만 쓴다.

- **단일 에이전트 (기본)** — 유저가 대화하던 LLM이 계획·실행·검증을 끝까지
  처리할 수 있고, 다른 에이전트의 독립 작업이나 교차 검증이 결과를 실질적으로
  바꿀 가능성이 낮은 작업.
- **팀(멀티에이전트)** — 독립적으로 분리할 수 있는 작업이 있거나, 현재
  에이전트와 다른 관점의 검증이 실제 위험을 줄이거나, 현재 에이전트가 막힌
  문제에 다른 도구·모델의 능력이 필요한 작업.
- **파일 수와 단계 수만으로 팀을 부르지 않는다.** 파일이 많아도 기계적인
  변경이면 단일 에이전트가 적합하고, 파일 하나라도 되돌리기 어렵거나 독립
  검증이 중요하면 팀을 고려할 수 있다.
- **모호하면 단일로 기운다.** 팀 호출에는 cold start, 컨텍스트 전달, 결과
  검토 비용이 추가된다.

### 팀 호출 판단 질문

다음 질문 중 하나 이상에 구체적으로 답할 수 있을 때만 팀을 고려한다.

1. 다른 에이전트에 맡길 수 있는 독립된 산출물이나 검증 질문이 있는가?
2. 다른 에이전트의 결과에 따라 구현·배포·승인 판단이 실제로 달라질 수 있는가?
3. 테스트·빌드·공식 문서 확인보다 다른 에이전트 호출이 더 적합한가?
4. 현재 에이전트에 없는 도구, 모델 특성, 컨텍스트가 필요한가?
5. 실패 시 복구가 어렵거나 다른 사용자·데이터에 영향을 주어 독립 검토가 필요한가?

어느 질문에도 구체적으로 답하지 못하면 현재 인터랙티브 에이전트가 단독으로
처리한다. 팀을 쓰기로 했다면 호출 프롬프트에 위 질문에서 확인한 목적을 적고,
막연한 전체 재검토보다 독립 작업이나 특정 위험 검토를 요청한다.

### 팀 호출 경계값

아래 기준은 기본 운영값이다. 다른 프로젝트에 이 템플릿을 병합하면서 기준을
바꿀 수 있지만, 바꾼 경우에는 프로젝트의 `README.md`나 `docs/`에 변경한 기준과
근거를 남긴다. 근거 없이 "상황에 따라 판단"이라고만 남기지 않는다.

다음 조건 중 2개 이상이 참이면 워커 호출을 고려한다.

- 독립 산출물로 분리 가능한 구현, 리뷰, 검증 단위가 있다.
- 보안, 데이터 손실, 대규모 리팩터링처럼 실패 비용이 크다.
- 다른 모델이나 도구의 관점이 구현 또는 승인 판단을 실제로 바꿀 수 있다.
- 현재 에이전트가 사용할 수 없는 세션, 인증 상태, 브라우저 상태, CLI가 필요하다.
- 테스트나 빌드만으로 위험이 충분히 줄지 않는다.
- 결과를 자동 분기하거나 승인 판단에 쓰려면 명확한 판정 신호가 필요하다.

다음 조건이 모두 참이면 단일 에이전트로 처리한다.

- 구현자와 검토자가 같아도 위험이 낮다.
- 테스트, 빌드, diff 확인으로 충분히 검증 가능하다.
- 워커 호출 비용이 결과 품질을 실질적으로 바꾸지 않는다.
- 작업 범위가 한 모듈이나 한 문서 안에 머문다.

### 예시

- **단일 에이전트로 충분한 경우** — README 문구 수정, 규칙 문서의 오탈자 수정, 테스트 없는 짧은 설정값 변경처럼 범위가 작고 교차 검증 비용이 더 큰 작업.
- **멀티에이전트가 맞는 경우** — 구현과 독립 보안 검토를 분리해야 하거나,
  대량 생성 작업을 별도 워커에 맡기는 동안 PM이 다른 판단을 진행하거나,
  현재 에이전트가 사용할 수 없는 도구가 필요한 작업.
- **경계 사례** — 파일 수는 적어도 설계 판단과 검증 기준이 길게 얽히면 멀티에이전트를 고려할 수 있다. 반대로 파일 수가 조금 많아도 기계적인 치환 수준이면 단일 에이전트로 끝낼 수 있다.

---

## 2. 팀 — 역할과 LLM

### 2.1 네 가지 역할

| 역할 | 하는 일 |
|---|---|
| **PM / Orchestrator** | goal 관리, 산출물 관리, 커밋, 조율, 진행 체크. **유저와 직접 대화.** |
| **Architect** | 단계별 구현 기획 |
| **Reviewer / Auditor** | 기획 리뷰, 구현 테스트·검증·감사 |
| **Coder** | 구현, 인터넷 탐색, 구글 서비스 친화 |

### 2.2 LLM ↔ 역할 매핑 · 폴백

사용 가능한 LLM 넷 중 **hermes-gemma는 성능·속도 이슈로 팀에서 배제**한다.

| LLM | 주 역할 | 리밋 시 대응 |
|---|---|---|
| **Claude Code** | 인터랙티브 PM 또는 워커 | 현재 세션 재개 또는 다른 인터랙티브 PM으로 수동 전환 |
| **codex** (GPT) | 인터랙티브 PM 또는 워커 | 현재 세션 재개 또는 다른 인터랙티브 PM으로 수동 전환 |
| **agy** (Gemini, 멀티모달·구글 친화, 사용량 리밋 가장 큼) | 인터랙티브 PM 또는 워커 | 현재 세션 재개 또는 다른 인터랙티브 PM으로 수동 전환 |
| ~~hermes-gemma~~ | (배제) | — |

- **PM은 사용자가 작업을 시작한 인터랙티브 세션이다.** Claude, codex, agy
  등 어느 LLM에서 시작해도 PM이 될 수 있다. 특정 모델을 기본 PM으로 고정하지
  않으며, 세션 리밋·작업 적합성·기존 맥락을 보고 사용자가 진입점을 고른다.
- **PM 전환은 자동 폴백이 아니라 사용자 주도의 새 인터랙티브 세션 시작이다.**
  기존 PM은 전환 전에 플랜·핸드오프 문서와 커밋을 최신화한다.

- **Architect(기획)는 기본적으로 PM이 직접 한다.** 유저와의 요구사항·제약
  대화 맥락이 PM에게 있고, 초안은 여러 번 반복 수정되는데 그 반복이
  인터랙티브 PM 루프에서 가장 매끄럽기 때문이다.
- **위임은 예외** — PM이 리밋이거나 기획 부하가 클 때만 다른 LLM에 위임한다.
  기획 단계엔 구현이 돌지 않아 워커가 idle이므로, PM이 **남은 리밋·부하를
  보고 여유 있는 LLM을 선정**한다. 단 위임하면 반복 수정의 맥락 전달 비용이 든다.
- 리뷰를 호출하는 경우에는 **기획·구현 담당과 다른 에이전트**가 맡는다
  (교차검증).
- **현재 PM만 유저와 직접 대화한다.** PM이 호출한 architect·reviewer·coder는
  headless 워커로 실행되며 유저와 직접 대화하지 않는다.

공급자와 실행 환경을 넘는 에이전트끼리는 대화 컨텍스트가 자동 공유되지
않는다. 따라서 오케스트레이션은 **현재 PM이 사용 가능한 CLI·도구로 다른
에이전트를 호출하는 soft orchestration**이다. 컨텍스트는 공유되지 않는다(5절).

### 2.3 역할은 "모델"이 아니라 "실행 모드"로 결정된다

같은 모델이라도 어떤 모드로 실행됐는지에 따라 역할이 다르다. 모든
에이전트에 동일하게 적용된다.

| 실행 모드 | 역할 | 행동 |
|---|---|---|
| **인터랙티브 진입점** (사람이 띄운 cli·desktop·ide) | PM 후보 | 단순하면 직접 처리하고, 팀이 필요하면 현재 세션이 PM으로 조율 |
| **PM** (인터랙티브) | 오케스트레이터 | 유저와 직접 대화하며 필요한 워커를 headless 호출 |
| **headless 워커** (`agy -p`·`codex exec` 등) | 작업자 | 지정된 단위작업만. **다른 외부 에이전트를 호출하지 않음** ← 루프 차단 |

- **PM은 headless가 아니라 인터랙티브 진입점이다.** 유저와 직접 대화하며
  진행한다. 따라서 **다른 LLM의 대화창 안에서 PM을 headless로 부르는 방식은
  쓰지 않는다**(그러면 PM이 유저와 직접 대화할 수 없으므로 PM 정의에 어긋남).
- **예외(무인 자동화)** — 유저가 자리를 비우고 완전 자동 실행을 원할 때만
  별도의 headless 오케스트레이터를 구동할 수 있다. 이땐 유저와 직접 대화
  없이 산출물로만 소통하며, 구현별 권한과 중단 조건을 명시한다.
- **단방향 규칙(루프 차단)** — headless 워커는 다른 외부 에이전트를 호출하지
  않는다. (codex가 프롬프트로 띄우는 내부 subagent는 codex 안에서만 도는
  것이라 외부 소환이 아니므로 예외.)

---

## 3. headless 호출 방법 (검증됨)

### codex (GPT)
- 비대화형 실행: `codex exec "<프롬프트>"`
- 읽기 전용(파일 수정 금지): `codex exec --config sandbox_mode=read-only "..."`
- 코드 리뷰 전용: `codex review` / `codex exec review`
- diff 적용: `codex apply` (Codex가 만든 diff를 `git apply`)
- 로그인 상태: `codex login status`
- 모델 지정: `-m <model>`, 이미지 첨부: `-i <file>`
- subagent 병렬: 프롬프트에 "각 항목마다 agent를 띄워 병렬로 …"처럼 **명시
  요청**할 때만 spawn. 토큰이 더 든다.

### agy (Gemini)
- 비대화형 실행: `agy -p "<프롬프트>"` (`--print`)
- 권한 자동 승인(쓰기 headless): `--dangerously-skip-permissions` (위험, 신중히)
- 샌드박스(터미널 제한): `--sandbox`
- 워크스페이스 디렉토리 추가: `--add-dir <dir>`
- 로그: `--log-file <path>`, print 타임아웃 기본 5분(`--print-timeout`)

### PM을 headless로 구동할 때 (무인 모드)
- `claude -p "<프롬프트>" --allowedTools "Bash"` — 에이전트 루프로 워커를
  호출·완수. 권한은 `--allowedTools`로 좁게 여는 것으로 충분(전체 우회 불필요).

### 공통 운영 주의 (검증 중 확인)
- **출력 깨짐 방지** — agy는 컬러·유니코드 출력이 많아 Bash 결과 처리를
  깨뜨릴 수 있다. `NO_COLOR=1`로 돌리고 **파일로 리다이렉트한 뒤 Read**한다.
- **agy 첫 실행 워밍업** — 임베디드 바이너리 설치·로그인 직후 초기화로 첫
  호출이 느리거나 timeout날 수 있다. 워밍업 후엔 빠르다. 타임아웃을 넉넉히.
- **비대화형 승인 제약** — codex·agy 모두 비대화형에서 승인이 필요한 동작은
  실패하거나 멈춘다. 리뷰·읽기는 안전, 쓰기는 승인 모드를 명시해야 한다.

---

## 4. 무결성·안전 가드 (필수)

여러 에이전트가 같은 워킹 트리를 동시에 만지거나, headless 워커가 통제 없이
도는 경우 사고가 난다. headless라 사람이 중간에 못 막는다는 점이 공통 위험이다.

### 무결성 (동시 작업)
- **쓰기는 한 번에 한 에이전트만.** 나머지는 read-only로 호출한다.
- **구현(쓰기) 단계는 git worktree/브랜치로 격리**하고, 머지 전 PM이 검토한다.
  충돌은 머지 시점에 git이 잡는다.

### 워커의 파괴적 행동 방지 (위험도 높음)
- headless 워커가 권한을 연 채(`--dangerously-skip-permissions`, codex sandbox
  write) 돌면 의도 밖 파일 삭제·위험 명령을 사람이 못 막는다.
- **기본은 read-only.** 쓰기가 필요한 워커만 **worktree로 격리한 뒤** 꼭 필요한
  최소 권한으로 연다. 격리 밖(공유 트리)에서 권한을 열지 않는다.

### 무인 모드 승인 공백 (위험도 높음)
- 무인 PM(`claude -p`)이 파괴적이거나 모호한 결정에 도달하면 사람에게 못
  물어본다.
- **승인이 필요한 작업은 무인 모드에서 임의로 진행하지 말고, 중단·기록 후
  보류**한다. 유저가 돌아와 판단한다.

### 무한 루프·폭주 차단
- PM은 "같은 지적 반복·diff가 안 줄어듦" 같은 **수렴 실패를 의미적으로 판단**해
  멈추고 유저에 에스컬레이션한다. 단 PM 자신이 루프의 일부면 자각이 약하다.
- **인터랙티브 PM** — 유저가 루프를 보고 멈출 수 있으므로 PM 판단으로 충분.
- **무인 PM/워커** — 자각을 못 믿으므로 **하드 한도(최대 반복 횟수·타임아웃)를
  하네스로 강제**한다(예: `timeout N <cmd>`). 이게 최후 방어선이다.

### 출력 형식 — 판정·제어 신호에만
- coder처럼 **파일 산출물(커밋·문서·diff)을 남기는 작업**은 PM이 그 파일을
  읽으면 되므로 출력 규약이 필요 없다.
- 반면 **PM이 결과로 자동 분기하는 판정·제어 신호**(reviewer 통과/반려,
  verifier 성공/실패)는 산문으로 받으면 분기 기준을 못 뽑는다. 이런 호출만
  **출력 형식을 프롬프트에 명시**한다(예: "마지막 줄에 `VERDICT=PASS|FAIL`").

### 알려진 이슈 (아직 규칙화 안 함, 겪으면 보강)
- **stale 컨텍스트** — 워커가 오래된 파일·플랜 기준으로 작업. 핸드오프 시
  "현재 git 상태·플랜 최신본 기준"을 명시해 완화.
- **비용 폭주** — subagent 병렬·반복 호출로 토큰 급증. 상한·모니터링 필요.
- **도구 드리프트** — codex·agy CLI 업데이트로 3절 호출 명령이 깨질 수 있음.
  깨지면 `--help`로 재확인. (3절 명령은 2026-06 검증 기준.)

---

## 5. 핸드오프 · 산출물 리뷰

각 외부 호출은 cold start다. 에이전트 간 메모리·대화 맥락은 공유되지 않는다.

- **공유 상태 = git repo + 해당 작업의 플랜 문서(`docs/plans/`) + 핸드오프 노트.**
- 호출 프롬프트에 컨텍스트(대상 파일 경로, 목표, 제약, 성공 기준)를 **명시**
  한다. "알아서 이어서"는 통하지 않는다.
- 외부 호출 결과(리뷰·검증·구현)는 PM이 **무비판 수용하지 않고** 가려서
  반영/반려를 판단하고, 그 근거를 플랜 문서에 남긴다.
- **유저 리뷰** — 워커는 유저와 직접 대화하지 않지만, 각 단계가 **산출물
  (파일)**을 남기므로 유저가 그 산출물을 읽고 리뷰할 수 있다. PM은 워커의
  작업 상태를 유저에게 피드백한다.
- **고급 기능(subagent 병렬 등)** — PM이 판단해 유저에게 제안하거나, 유저가
  PM에게 제안하고 PM이 검토해 거절 의견을 낼 수 있다.

### PM 로밍 핸드오프

PM 로밍은 기존 대화 세션을 다른 에이전트로 그대로 복제하는 기능이 아니다.
Claude, codex, agy 사이에서는 내부 대화 히스토리, 숨은 상태, 도구 호출 맥락이
자동 공유되지 않는다. 로밍은 새 인터랙티브 PM이 저장소 상태를 읽고 작업을
복원하는 절차다.

따라서 로밍 가능한 상태의 SSOT는 다음 순서로 해석한다.

1. 현재 git 상태와 마지막 커밋.
2. 해당 작업의 `docs/plans/` 플랜 파일.
3. 플랜의 `Handoff Snapshot`과 `Context Notes`.
4. 필요할 때만 이전 PM의 대화 요약이나 별도 핸드오프 노트.

새 PM을 시작할 때는 다음 형태의 프롬프트를 기준으로 한다.

```text
이 저장소의 AGENTS.md 또는 CLAUDE.md를 읽고 규칙을 따른다.
현재 작업은 docs/plans/<파일명>에 있다.
너는 새 인터랙티브 PM이다.
먼저 플랜의 Handoff Snapshot, Context Notes, git status를 읽고,
이전 PM의 마지막 상태를 복원한 뒤 다음 작업만 진행한다.
기존 변경을 되돌리지 말고, 불명확하면 질문한다.
```

로밍을 받은 새 PM은 바로 구현하지 않는다. 먼저 플랜과 git 상태를 읽어 현재
작업, 미커밋 변경, 마지막 검증, 다음 안전한 단계를 확인한다. 이전 PM의 판단이
불명확하면 추정으로 덮지 말고 유저에게 확인한다.

### 핸드오프 보조 명령·스킬

수동 핸드오프를 반복할 일이 많으면 에이전트별 스킬이나 로컬 명령으로 절차를
감싸는 것이 좋다. 단 이 도구의 책임은 핸드오프를 쉽게 만드는 것이지, 새 PM의
판단을 자동 대체하는 것이 아니다.

권장 명령은 두 가지로 나눈다.

- `handoff prepare` — 현재 PM이 넘기기 전에 실행한다. git 상태, 현재 브랜치,
  마지막 커밋, 변경 파일 목록, 최근 검증 명령을 모으고 플랜의
  `Handoff Snapshot` 갱신을 요구한다.
- `handoff receive` — 새 PM이 이어받을 때 실행한다. `AGENTS.md` 또는
  `CLAUDE.md`, 대상 플랜, `Handoff Snapshot`, `git status`를 읽고 바로 구현하지
  말아야 한다는 시작 절차를 확인한다.

자동 수집해도 되는 값은 객관 상태로 제한한다. 예를 들면 현재 브랜치, 마지막
커밋, 미커밋 파일, 원격 동기화 여부, 실행한 검증 명령이다. 사용자의 실제 목표,
현재 의도, 다음 안전한 행동, 보류한 결정은 PM이 짧게 직접 적는다.

스킬로 만들 경우에도 두 명령은 같은 템플릿 문서를 기준으로 동작해야 한다.
프로젝트마다 별도 문체나 필드를 만들지 않고 `workflow-rules.md`의
`Handoff Snapshot` 형식을 따른다.

---

## 6. 표준 워크플로 (복잡 작업)

1. **PM (현재 인터랙티브 세션)** — `workflow-rules.md`에 따라 해당 작업의
   플랜 작성. goal·산출물 정의.
2. **Architect (기본 PM 직접, 위임은 예외)** — 단계별 구현 기획. 유저
   요구사항 대화 + 초안 반복 수정이 잦아 보통 PM이 직접 작성. PM 리밋·부하
   시에만 리밋 여유 있는 LLM에 위임(리뷰는 기획자와 다른 LLM).
3. **Coder (PM이 선정한 워커 또는 PM 직접)** — 필요하면 격리된
   브랜치/worktree에서 구현.
4. **Reviewer (기획·구현 담당과 다른 에이전트)** — 독립 검토가 필요한
   경우에만 기획 리뷰 또는 구현 테스트·검증 (read-only).
5. **PM (현재 인터랙티브 세션)** — 결과 가려 반영, 머지, 유저 피드백. **매 의미 단위마다
   플랜·체크리스트 갱신 + 커밋**(7절 체크포인트).

---

## 7. 리밋·실패 대응

구독 요금제 리밋은 예고 없이 온다. 예측하려 하지 말고, 아래 두 축으로 대비한다.

### 예방 — 체크포인트
- **PM은 리밋을 예측하려 하지 않는다.** 대신 **매 의미 단위마다 플랜 문서
  (체크리스트·Context Notes)를 갱신하고 커밋**한다. 그러면 리밋이 *언제*
  오든 손실이 "마지막 체크포인트 이후 진행분"으로만 한정된다.
- 어디서 끊겨도 다음 PM(폴백 또는 복귀)이 플랜 문서를 읽고 이어받는다.
- 체크포인트는 리밋 알림이 아니라 작업 트랜잭션을 기준으로 한다. 파일 수정
  전후, 워커 호출 전후, 검증 후, 사용자 중간 보고 전, 의미 단위 커밋 전후에
  플랜의 `Handoff Snapshot` 갱신을 우선 고려한다.
- 자동화가 필요하면 git 상태, 현재 브랜치, 마지막 커밋, 변경 파일 목록 같은
  객관 상태 수집까지만 맡긴다. 다음 의도와 안전한 후속 행동은 PM이 짧게
  기록한다.

### 대응 — 현재 세션 재개 또는 유저 주도 전환
- **PM 리밋** — 기존 맥락의 가치가 크고 기다릴 수 있으면 같은 세션을
  재개한다. 작업 전환이 필요하면 플랜·핸드오프 문서와 커밋을 기준으로 다른
  인터랙티브 에이전트에서 새 PM 세션을 시작한다.
- **유저가 더 대기할 수 없는 사정이면** — 그때 **PM을 교체**한다. 유저가
  다른 LLM을 PM으로 새로 시작하고, 맥락은 플랜 문서에서 복원한다.
- **기본 운영에는 자동 폴백이 없다.** PM 교체 트리거는 유저가 직접 판단한다.
  별도 하네스로 자동 전환을 구현할 수는 있지만, 승인 공백과 잘못된 상태
  인계를 다룰 수 있을 때만 도입한다.
- **워커 리밋** — PM은 살아 있으므로 PM이 대기 또는 다른 워커로 대체를
  결정한다(이건 트리거 주체가 있어 문제없다).
- **로밍 후 복귀** — 원래 PM이 나중에 복귀하면 새 PM이 남긴 커밋, 플랜,
  `Handoff Snapshot`을 기준으로 이어받는다. 대화 기억보다 저장소 상태를 우선한다.

---

## 8. 비용·외부 전송 주의

- subagent·멀티 호출은 토큰을 더 쓴다. 정액 구독으로 상쇄되나 rate limit을
  고려한다.
- codex·agy 호출은 코드·문서를 **OpenAI·Google로 전송**한다. 민감 정보가
  외부로 나가지 않도록 호출 범위를 관리한다.

---

## 9. 검증 현황 / 후속

- **검증됨 (2026-06-04)**.
  - codex 1-hop — `codex exec`로 플랜 리뷰 성공.
  - agy 1-hop — `agy -p` 응답 성공(첫 실행 워밍업 후).
  - **claude -p → codex 2-hop** — `claude -p`가 PM으로 떠서 Bash로
    `codex exec`를 호출·파싱·보고. claude headless가 에이전트 루프로
    오케스트레이션을 끝까지 완수함을 실증. 권한은 `--allowedTools "Bash"`로 충분.
- **미검증** — claude → agy 2-hop, 실전 다단계 파이프라인 연결, worktree
  격리 실측, 쓰기 작업 headless 안전성, 진입점(desktop/ide) → claude 호출.
- **정함** — Architect(기획)는 기본 PM이 직접 수행, 위임은 PM이 리밋·부하
  보고 선정 (2.2·6절).
- **연결 완료** — codex·agy 공통 진입 파일 `AGENTS.md`(이 문서를 참조).
  Claude는 `CLAUDE.md`가 참조. 셋 다 얇은 포인터이며 실제 규칙은 이 문서와
  `workflow-rules.md`·`karpathy-plus.md`에 둔다(SSOT).
- **보류 결정** — 로컬 gemma(LM Studio) 합류: VRAM 6G에 12B Q4가 안 올라가
  CPU offload로 느려져, "대량·빠른 워커"라는 유일한 실익이 사라짐. 적합한
  역할이 없어 팀에 넣지 않음. 이는 멀티에이전트 팀 멤버로 쓰는 경우에 대한
  결정이며, 긴 입력을 줄이는 로컬 컨텍스트 라우터는 `local-context-routing.md`를
  따른다. 하드웨어·모델 조건이 바뀌면 재검토.
- 글로벌(~/.claude) 승격은 실전 검증 이후 검토.
